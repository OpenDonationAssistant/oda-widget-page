import { observer } from "mobx-react-lite";
import { TimedElementSettings } from "./TimedElement";

export const TimedElementRenderer = observer(
  ({ settings }: { settings: TimedElementSettings }) => {
    return <></>;
  },
);
